random_seed: 0
exp_dir: '../exp/'
data_dir: '../data/'
exp_name: 'test'

data:  
  train_prices_decisions: 'train_prices_decisions.csv'
  train_covariate: 'train_covariate'
  train_noisy_embedding: 'train_noisy_embedding'
  item0embedding: 'item0embedding'
  item1embedding: 'item1embedding'

strategy: BaseStrategy

preprocess:
  preprocessor: 'KNN_Preprocessor'
  param:
    n_neighbors: 5

optimize:
  price_range: []
  fold: 5
  model: 'Logistic_Regression'
  param:
    max_iter: 100
